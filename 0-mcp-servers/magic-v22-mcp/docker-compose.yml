services:
  mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: magic-v22-mcp
    ports:
      - "9898:9898"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
